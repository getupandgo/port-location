excludes:
  - .vendor

protoc:
  version: 3.8.0

generate:
  go_options:
    import_path: github.com/getupandgo/port-location/api/proto
  plugins:
    - name: go
      type: go
      flags: plugins=grpc,paths=source_relative
      output: .

lint:
  group: uber2
